@model sklad56.Models.Place

@{
    ViewBag.Title = "Редактирование места";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var Edit = (bool)ViewBag.Edit;
}

<div class="row">
    <div class="col-sm-2">

    </div>
    <div class="col-sm-10 top-menu">
        <div class="tabbable">
            <ul class="nav nav-tabs">
                <li><a href="@Url.Action("Index", "Home")">Главная</a></li>
                <li class="active"><a href="@Url.Action("Editor", "Home")">Редактирование</a></li>
                <li><a href="@Url.Action("Movements", "Home")">Операции</a></li>
            </ul>
        </div>
    </div>
    <div class="col-sm-2 sidebar">
        <ul class="nav nav-pills nav-stacked">
            <li>@Html.ActionLink("пользователи", "UserEditList", "User")</li>
            <li>@Html.ActionLink("предметы", "EquipEditList", "Equip")</li>
            <li>@Html.ActionLink("комплекты", "EditList", "Package")</li>
            <li>@Html.ActionLink("места", "EditPlaceList", "Package")</li>
        </ul>
    </div>
    <div class="col-sm-8 content">
        <h3>Создание/редактирование места</h3>

        @using (Html.BeginForm("RegisterPlace", "Package", new { Edit = Edit }, FormMethod.Post, new { @class = "form-horizontal" }))
        {
            <fieldset>
                <div class="control-group">
                    @Html.HiddenFor(x => x.ID_Place)
                    <label class="control-label" for="Placename">
                        <abbr title="Данное имя будет отображаться везде, где фигурирует место" class="initialism">Имя места</abbr>
                    </label>
                    <div class="controls">
                        @Html.TextBox("Name", Model.Name)
                        @Html.ValidationMessage("Name")
                    </div>
                </div>
                <div class="control-group">
                    <label class="control-label" for="City">
                        <abbr title="Населенный пункт, в котором располагается место." class="initialism">Город</abbr>
                    </label>
                    <div class="controls">
                        @Html.TextBox("City", Model.City)
                        @Html.ValidationMessage("City")
                    </div>
                </div>
                <div class="control-group">
                    <label class="control-label" for="Picture">
                        <abbr title="Имя файла, расположенного в папке Content/img. Поле не обязательное" class="initialism">Фото места</abbr>
                    </label>
                    <div class="controls">
                        @Html.TextBox("Picture", Model.Picture)
                    </div>
                </div>
                <br />
                <div class="form-actions">
                    <button type="submit" class="btn btn-success">
                        Применить
                    </button>
                    @Html.ActionLink("Cancel", "EditPlaceList", "Package", null, new { @class = "btn btn-primary" })
                </div>
            </fieldset>
        }
    </div>
</div>
